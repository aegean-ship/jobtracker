package dev.aegeanship.jobtracker.userservice.user.enums;

/**
 * Enum representing user roles in the system.
 * Used for role-based access control (RBAC) to determine permissions and access levels.
 */
public enum Role {
    ADMIN,
    USER
}
